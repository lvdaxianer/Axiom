# Saga 补偿框架架构设计方案

> **框架名称**: saga-compensation-spring-boot-starter
> **版本**: v1.1
> **作者**: lvdaxianer@yeah.net
> **日期**: 2026-09-04
> **适用场景**: Spring Boot 3 / Java 17 项目中的本地数据库或 Elasticsearch 业务数据 + 下游 HTTP/RPC 调用

---

## 一、背景与问题

### 1.1 典型场景

在微服务或分布式系统中，经常遇到"本地事务 + 远程 HTTP 调用"的场景：

**场景1：创建订单**
```
1. 本地数据库插入订单记录 (orderId=123)
2. HTTP 调用库存服务扣减库存
3. HTTP 调用支付服务创建支付单
```

**场景2：用户注册**
```
1. 本地数据库插入用户记录 (userId=456)
2. HTTP 调用邮件服务发送欢迎邮件
3. HTTP 调用积分服务初始化积分账户
```

**场景3：删除资源**
```
1. HTTP 调用第三方服务删除远程资源
2. HTTP 调用对象存储删除文件
3. 本地数据库删除记录
```

### 1.2 核心问题

**问题1：原子性无法保证**
- 本地数据库操作成功，但 HTTP 调用失败 → 数据不一致
- HTTP 调用成功，但本地操作失败 → 数据不一致

**问题2：级联失败**
```
本地插入 ✅ → 服务A调用 ✅ → 服务B调用 ❌
结果：本地和服务A有数据，但服务B没有 → 需要级联回滚
```

**问题3：补偿复杂**
- 手动补偿容易遗漏
- 缺乏统一的补偿机制
- 补偿逻辑与业务代码耦合

**问题4：可观测性差**
- 难以追踪事务执行状态
- 补偿失败后难以定位问题

### 1.3 设计目标

本框架旨在提供一个**通用的、可插拔的**分布式事务补偿解决方案：

✅ **通用性**：不绑定任何具体业务，适用于任何 Spring Boot 项目
✅ **低侵入**：只需加注解，最小化代码改动
✅ **最终一致性**：异步执行，用户体验好
✅ **自动补偿**：失败自动重试 + 回滚
✅ **可观测**：完整的日志、监控、告警
✅ **易扩展**：插件化设计，支持自定义策略

---

## 二、架构设计

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                         业务层 (Service)                          │
│                                                                   │
│  @SagaTransaction(name="createScene", businessKey="#id")  │
│  public SceneEntity createScene(SceneDTO dto) {                  │
│    // 1. 本地数据库操作                                           │
│    SceneEntity scene = sceneDao.insert(...);                     │
│                                                                   │
│    // 2. 注册远程调用（不立即执行）                               │
│    sagaContext.registerRemote("thingui", ...);                   │
│    sagaContext.registerRemote("topo", ...);                      │
│                                                                   │
│    return scene;                                                 │
│  }                                                               │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    AOP 拦截层 (@Aspect)                          │
│                                                                   │
│  ┌─ 方法执行前 ────────────────────────────────────┐            │
│  │ • 创建 Saga 上下文                               │            │
│  │ • 生成全局事务 ID                                │            │
│  └──────────────────────────────────────────────────┘            │
│                                                                   │
│  ┌─ 方法执行中 ────────────────────────────────────┐            │
│  │ • 业务方法执行（本地操作 + 注册远程调用）        │            │
│  │ • 远程调用暂不执行，只记录在上下文中             │            │
│  └──────────────────────────────────────────────────┘            │
│                                                                   │
│  ┌─ 本地事务提交前 (beforeCommit) ────────────────┐            │
│  │ • 持久化补偿任务到数据库                         │            │
│  │   - 主表: saga_transaction_log            │            │
│  │   - 明细: saga_transaction_step           │            │
│  └──────────────────────────────────────────────────┘            │
│                                                                   │
│  ┌─ 本地事务提交后 (afterCommit) ─────────────────┐            │
│  │ • 异步执行所有远程调用（线程池并行）             │            │
│  │ • 更新步骤状态 (SUCCESS/FAILED)                  │            │
│  │ • 全部成功则持久化 SUCCESS 终态，按保留策略归档   │            │
│  └──────────────────────────────────────────────────┘            │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    异步执行层 (ExecutorService)                  │
│                                                                   │
│  线程池并行执行：                                                │
│  ┌────────────────┐        ┌────────────────┐                   │
│  │  Task 1        │        │  Task 2        │                   │
│  │  调用森大屏     │        │  调用 Topo      │                   │
│  │  ✅ SUCCESS    │        │  ❌ FAILED     │                   │
│  └────────────────┘        └────────────────┘                   │
│                                                                   │
│  结果：                                                           │
│  • 成功：更新 step.status = SUCCESS，保存返回值                  │
│  • 失败：更新 step.status = FAILED，记录错误信息                 │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                   补偿调度层 (@Scheduled)                        │
│                                                                   │
│  定时任务（每30秒执行一次）：                                     │
│                                                                   │
│  ┌─ 阶段1: 重试失败步骤 ────────────────────────┐               │
│  │ • 查询 status=FAILED 且 retry_count < max    │               │
│  │ • 重新执行远程调用                           │               │
│  │ • retry_count++                              │               │
│  └──────────────────────────────────────────────┘               │
│                                                                   │
│  ┌─ 阶段2: 触发补偿（反向回滚）─────────────────┐               │
│  │ • 查询 retry_count >= max 的事务             │               │
│  │ • 按步骤倒序执行补偿：                       │               │
│  │   1. 删除森大屏 (compensate)                 │               │
│  │   2. 删除 Topo (compensate)                  │               │
│  │   3. 删除本地数据 (delete by businessKey)    │               │
│  │ • 更新事务状态 = COMPENSATED                 │               │
│  └──────────────────────────────────────────────┘               │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 核心设计原则

#### 原则1：最终一致性（异步执行）

```
用户请求 → 本地操作提交 → 立即返回成功 → 后台异步执行远程调用
```

**优势**：
- 用户体验好（不需要等待远程调用）
- 本地操作快速完成
- 远程调用失败不影响用户操作

**劣势**：
- 短时间内数据可能不一致
- 需要业务方理解最终一致性语义

#### 原则2：补偿优于回滚（Saga模式）

不使用传统的两阶段提交（2PC），而是采用 **Saga 补偿模式**：

```
正向执行：
Step1(本地) → Step2(森大屏) → Step3(Topo)
   ✅            ✅              ❌

反向补偿：
Step3.compensate() → Step2.compensate() → Step1.compensate()
  跳过(未创建)        删除森大屏 ✅          删除本地 ✅
```

#### 原则3：幂等性保证

虽然远程接口不支持幂等，但框架层通过以下机制保证：

1. **状态机控制**：每个步骤只能从 PENDING → SUCCESS/FAILED
2. **数据库唯一约束**：防止重复插入补偿任务
3. **分布式锁**：执行前先获取锁，避免并发重复执行

#### 原则4：低侵入性

业务代码改造最小化：
- 只需添加 `@SagaTransaction` 注解
- 使用 `SagaContext` 注册远程调用
- 其他代码保持不变

---

## 三、数据库设计

### 3.1 补偿任务主表

```sql
CREATE TABLE `saga_transaction_log` (
  `id` VARCHAR(32) PRIMARY KEY COMMENT '事务ID (UUID)',
  `business_key` VARCHAR(64) NOT NULL COMMENT '业务唯一键 (如 orderId)',
  `transaction_name` VARCHAR(128) NOT NULL COMMENT '事务名称 (如 createOrder)',
  `status` VARCHAR(20) NOT NULL DEFAULT 'PENDING' COMMENT '事务状态',
  `retry_count` INT NOT NULL DEFAULT 0 COMMENT '重试次数',
  `max_retry` INT NOT NULL DEFAULT 5 COMMENT '最大重试次数',
  `error_message` TEXT COMMENT '最后一次错误信息',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `created_by` VARCHAR(64) COMMENT '创建人',

  INDEX `idx_business_key` (`business_key`),
  INDEX `idx_status` (`status`),
  INDEX `idx_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='Saga 事务日志表';

-- 状态枚举:
-- PENDING: 待执行
-- EXECUTING: 执行中
-- SUCCESS: 全部成功
-- PARTIAL_SUCCESS: 部分成功
-- FAILED: 全部失败
-- COMPENSATING: 补偿中
-- COMPENSATED: 已补偿
```

### 3.2 补偿步骤明细表

```sql
CREATE TABLE `saga_transaction_step` (
  `id` VARCHAR(32) PRIMARY KEY COMMENT '步骤ID (UUID)',
  `transaction_id` VARCHAR(32) NOT NULL COMMENT '关联事务ID',
  `step_order` INT NOT NULL COMMENT '步骤顺序 (1, 2, 3...)',
  `step_name` VARCHAR(128) NOT NULL COMMENT '步骤名称 (如 create_payment)',
  `step_type` VARCHAR(20) NOT NULL COMMENT '步骤类型 (LOCAL/REMOTE)',
  `service_name` VARCHAR(64) COMMENT '服务名称 (inventory, payment, email)',

  -- 执行信息
  `execute_class` VARCHAR(256) COMMENT '执行类全限定名',
  `execute_method` VARCHAR(128) COMMENT '执行方法名',
  `execute_params` TEXT COMMENT '执行参数 (JSON)',
  `execute_result` TEXT COMMENT '执行结果 (JSON)',

  -- 补偿信息
  `compensate_class` VARCHAR(256) COMMENT '补偿类全限定名',
  `compensate_method` VARCHAR(128) COMMENT '补偿方法名',
  `compensate_params` TEXT COMMENT '补偿参数 (JSON)',

  -- 状态信息
  `status` VARCHAR(20) NOT NULL DEFAULT 'PENDING' COMMENT '步骤状态',
  `retry_count` INT NOT NULL DEFAULT 0 COMMENT '重试次数',
  `error_message` TEXT COMMENT '错误信息',
  `executed_at` DATETIME COMMENT '执行时间',
  `compensated_at` DATETIME COMMENT '补偿时间',

  FOREIGN KEY (`transaction_id`) REFERENCES `saga_transaction_log`(`id`) ON DELETE CASCADE,
  INDEX `idx_transaction_id` (`transaction_id`),
  INDEX `idx_status` (`status`),
  INDEX `idx_step_order` (`step_order`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='Saga 事务步骤表';

-- 状态枚举:
-- PENDING: 待执行
-- EXECUTING: 执行中
-- SUCCESS: 执行成功
-- FAILED: 执行失败
-- COMPENSATING: 补偿中
-- COMPENSATED: 已补偿
-- SKIPPED: 跳过 (未执行的步骤无需补偿)
```

### 3.3 MySQL 模式：集群模式下事务表的归属

当 `saga.transaction.store-type=mysql`（默认）时，`saga_transaction_log` 和 `saga_transaction_step` 不单独部署到 Redis、每个应用节点的本地库，或公共的 Saga 中心库。它们应当与**发起 Saga 的业务服务**使用同一个逻辑业务数据库和同一个写数据源。

以创建订单为例，订单服务的 `order_db` 中同时包含 `orders`、`saga_transaction_log` 和 `saga_transaction_step`。订单服务的所有集群实例均连接这一个 `order_db`；库存、支付等被调用服务不写入订单服务的 Saga 表。若其他服务也需要发起自己的 Saga，则在各自业务库中创建各自的 `saga_*` 表，而不是多个服务共用一套中心表。

```
┌─────────────── 订单服务集群 ───────────────┐
│  Node 1             Node 2             Node 3 │
└─────────┬─────────────┬─────────────┬─────────┘
          └─────────────┴─────────────┘
                        │ 写入和任务扫描
                        ▼
          ┌──────────────────────────────┐
          │ order_db（逻辑业务库）         │
          │ - orders                      │
          │ - saga_transaction_log        │
          │ - saga_transaction_step       │
          └──────────────────────────────┘
```

#### 为什么必须同库同事务

业务方法执行时，订单等业务数据与 Saga 主表、步骤表必须由同一个 Spring 本地事务提交：

1. 本地业务写入成功时，Saga 任务记录同时提交，集群中任一节点随后都能接管执行。
2. 本地事务回滚时，Saga 任务记录也随之回滚，不会出现没有业务数据却被调度的“孤儿任务”。
3. 如果将 Saga 表拆到独立数据库，业务数据与任务记录之间不再具备本地原子性，需要额外引入 Outbox、可靠消息或独立协调器；这超出本框架的轻量嵌入式范围。

#### 集群访问与高可用约束

- 数据库高可用以业务库自身的主备、MGR 或云数据库集群实现；应用始终写当前主库，故障切换后继续使用同一个逻辑库。
- `saga_*` 表的插入、状态迁移、重试次数更新和待执行任务扫描必须走主库。只读副本只能用于监控、后台查询等允许短暂延迟的场景。
- Redis 分布式锁只用于决定哪个节点执行本轮调度，不能保存事务状态，也不能替代数据库的状态机和 CAS 更新。
- 节点在远程调用期间故障时，其他节点应先根据事务状态、超时阈值和远端幂等键判断是否可重试。远端执行结果未知且没有幂等或查询能力时，任务必须转人工处理，不能盲目重复调用。

### 3.4 Elasticsearch 模式：双索引事务状态源

当业务主数据已经由 Elasticsearch 保存，且不存在可共享的关系型本地事务边界时，配置
`saga.transaction.store-type=elasticsearch`。事务状态不落 MySQL，而是保存在与业务索引同一
ES 集群的两个**应用专属**索引中：

| 索引 | 内容 | 关键约束 |
|------|------|----------|
| `saga-transaction-log-v1` | 事务头、业务键、状态、重试次数、执行租约 | 事务 ID 由稳定业务键确定性生成，创建使用 `op_type=create` |
| `saga-transaction-step-v1` | 正向/补偿步骤载荷、步骤状态、步骤租约 | 与事务头分离，便于按步骤 CAS 抢占和恢复；载荷不得包含密码、令牌等凭据 |

该模式的版本基线为 **Elasticsearch 7.10.2**，使用应用提供的 `RestHighLevelClient`。Starter 不创建
客户端、不保存 ES 凭据；客户端缺失时启动失败，绝不静默回退到 MySQL。生产索引由部署流程根据
`elasticsearch/saga-transaction-index-7.10.2.json` 和
`elasticsearch/saga-step-index-7.10.2.json` 预创建；
`saga.transaction.elasticsearch.schema-initialize=false` 保持默认值。

```
┌──────────────────── 云瞰服务集群 ─────────────────────┐
│ Node 1                 Node 2                 Node 3 │
└─────────────┬───────────┬───────────┬───────────────┘
              │           │           │ ES 条件写入 / mget
              ▼           ▼           ▼
       ┌────────────────────────────────────────────┐
       │ Elasticsearch 7.10.2 集群                   │
       │ - organize-member（业务用户）                │
       │ - saga-transaction-log-v1（Saga 事务头）     │
       │ - saga-transaction-step-v1（Saga 步骤）      │
       └────────────────────────────────────────────┘
```

ES 不能与 Spring 关系型本地事务形成原子提交，因此它只提供最终一致性，调用方不得在活动
Spring 本地事务中启动 ES Saga。正确顺序是：

1. 业务先写入可恢复的中间状态，并写入确定性 `sagaTransactionId`；
2. 以稳定业务键创建或复用 ES Saga 事务；重复提交复用原事务，不重复调用下游；
3. Saga 正向步骤按租约和 CAS 执行，下游必须使用 `transactionId:stepId` 幂等键；
4. 成功或重试耗尽后的失败由赢得终态 CAS 的节点发布一次终态事件；
5. 若进程内终态通知丢失，集群 leader（或无 leader 组件的单实例）定时批量 `mget` 回查事务终态，再重放业务侧幂等状态转换。

Redis（若启用）只协调恢复扫描，不能保存 Saga 状态，也不能替代 ES 的条件状态迁移。每个发起
Saga 的服务拥有自己的索引前缀；不同业务服务不得共用无边界的中心 Saga 索引。

### 3.5 数据示例

```sql
-- 场景：创建订单，调用库存和支付服务

-- 主表记录
INSERT INTO saga_transaction_log VALUES (
  'tx_20260904100001',
  'order_123',
  'createOrder',
  'PARTIAL_SUCCESS',
  0,
  5,
  'Payment service timeout after 5 retries',
  '2026-09-04 10:00:01',
  '2026-09-04 10:05:30',
  'admin'
);

-- 步骤1: 扣减库存 (成功)
INSERT INTO saga_transaction_step VALUES (
  'step_001',
  'tx_20260904100001',
  1,
  'deduct_inventory',
  'REMOTE',
  'inventory',
  'com.example.client.InventoryClient',
  'deduct',
  '{"productId":"prod_456","quantity":2}',
  '{"lockId":"lock_789"}',
  'com.example.client.InventoryClient',
  'release',
  '{"lockId":"lock_789"}',
  'SUCCESS',
  0,
  NULL,
  '2026-09-04 10:00:02',
  NULL
);

-- 步骤2: 创建支付单 (失败)
INSERT INTO saga_transaction_step VALUES (
  'step_002',
  'tx_20260904100001',
  2,
  'create_payment',
  'REMOTE',
  'payment',
  'com.example.client.PaymentClient',
  'create',
  '{"orderId":"order_123","amount":99.99}',
  NULL,
  'com.example.client.PaymentClient',
  'cancel',
  NULL,
  'FAILED',
  5,
  'java.net.SocketTimeoutException: Read timed out',
  '2026-09-04 10:00:02',
  NULL
);
```

---

## 四、技术选型

### 4.1 核心依赖

| 技术栈 | 版本 | 用途 |
|--------|------|------|
| Spring Boot | 3.x | 基础框架 |
| Spring AOP | 自带 | 拦截 @SagaTransaction |
| Spring Transaction | 自带 | 本地事务管理 |
| Spring Task | 自带 | 定时补偿任务 |
| MyBatis-Plus | 3.5.x | ORM 框架 |
| Hutool | 5.8.x | 工具库（JSON、UUID） |
| ThreadPoolExecutor | JDK | 异步执行远程调用 |
| Redisson | 3.x (可选) | 分布式锁（防止重复补偿）|

### 4.2 不使用的技术（说明）

| 技术 | 不使用原因 |
|------|-----------|
| Seata | 过重，需要独立部署 TC 服务器；对现有代码侵入性大 |
| RocketMQ 事务消息 | 需要引入消息队列，增加系统复杂度 |
| Flowable/Camunda | 工作流引擎过重，学习成本高 |
| Spring Cloud Alibaba | 项目未使用微服务架构 |

### 4.3 线程池配置

```java
/**
 * 远程调用线程池配置
 */
@Configuration
public class RemoteCallExecutorConfig {

    @Bean("remoteCallExecutor")
    public ExecutorService remoteCallExecutor() {
        return new ThreadPoolExecutor(
            10,                           // 核心线程数
            50,                           // 最大线程数
            60L, TimeUnit.SECONDS,        // 空闲线程存活时间
            new LinkedBlockingQueue<>(200), // 队列容量
            new ThreadFactoryBuilder()
                .setNameFormat("remote-call-%d")
                .build(),
            new ThreadPoolExecutor.CallerRunsPolicy() // 拒绝策略
        );
    }
}
```

---

## 五、核心接口设计

### 5.1 注解定义

```java
/**
 * Saga 事务注解
 *
 * @author lvdaxianer@yeah.net
 * @date 2026-09-04
 */
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
@Documented
public @interface SagaTransaction {

    /**
     * 事务名称（用于日志和监控）
     * 默认取方法名
     */
    String name() default "";

    /**
     * 业务键表达式（SpEL）
     *
     * 示例：
     * - #result.id              (返回值的 id 字段)
     * - #dto.orderId            (参数的 orderId 字段)
     * - #p0.id                  (第一个参数的 id 字段)
     * - T(UUID).randomUUID()    (生成 UUID)
     */
    String businessKey();

    /**
     * 是否异步执行远程调用
     *
     * - true: 本地事务提交后，异步执行远程调用（推荐）
     * - false: 阻塞等待所有远程调用完成
     */
    boolean asyncRemote() default true;

    /**
     * 最大重试次数
     */
    int maxRetry() default 5;

    /**
     * 重试间隔（秒）
     */
    int retryInterval() default 30;

    /**
     * 是否自动补偿（删除本地数据）
     */
    boolean autoCompensate() default true;
}
```

### 5.2 Saga 上下文接口

```java
/**
 * Saga 事务上下文
 *
 * 生命周期：
 * 1. AOP 拦截方法前创建
 * 2. 方法执行中注册远程调用
 * 3. 本地事务提交后执行远程调用
 * 4. 方法执行完毕后销毁
 *
 * @author lvdaxianer@yeah.net
 * @date 2026-09-04
 */
public interface SagaContext {

    /**
     * 获取当前事务 ID
     */
    String getTransactionId();

    /**
     * 注册远程调用（并行执行）
     *
     * @param serviceName 服务名称（thingui, topo）
     * @param action 远程调用逻辑
     * @param compensate 补偿逻辑
     * @param <T> 返回值类型
     *
     * 示例：
     * sagaContext.registerRemote("thingui",
     *     () -> thinguiClient.createScene(sceneId, name),
     *     thinguiId -> thinguiClient.deleteScene(thinguiId)
     * );
     */
    <T> void registerRemote(String serviceName,
                            RemoteAction<T> action,
                            CompensateAction<T> compensate);

    /**
     * 获取某个步骤的执行结果（用于步骤间依赖）
     *
     * 注意：只能在本地事务提交后、远程调用完成后才能获取
     *
     * @param serviceName 服务名称
     * @return 执行结果（可能为 null）
     */
    <T> T getStepResult(String serviceName);

    /**
     * 判断是否所有步骤都成功
     */
    boolean isAllSuccess();

    /**
     * 手动触发补偿（慎用）
     */
    void compensate();
}

/**
 * 远程调用接口
 */
@FunctionalInterface
public interface RemoteAction<T> {
    /**
     * 执行远程调用
     *
     * @return 返回值（会保存到补偿任务表）
     * @throws Exception 执行异常
     */
    T execute() throws Exception;
}

/**
 * 补偿接口
 */
@FunctionalInterface
public interface CompensateAction<T> {
    /**
     * 执行补偿操作
     *
     * @param result 原始调用的返回值
     * @throws Exception 补偿异常
     */
    void compensate(T result) throws Exception;
}
```

### 5.3 补偿任务管理接口

```java
/**
 * 补偿任务管理服务
 *
 * @author lvdaxianer@yeah.net
 * @date 2026-09-04
 */
public interface CompensationService {

    /**
     * 保存补偿任务
     *
     * @param transactionLog 事务主记录
     * @param steps 步骤列表
     */
    void saveCompensationTask(TransactionLog transactionLog,
                              List<TransactionStep> steps);

    /**
     * 查询待重试的步骤
     *
     * @return 失败且未达到最大重试次数的步骤
     */
    List<TransactionStep> findPendingRetrySteps();

    /**
     * 查询需要补偿的事务
     *
     * @return 重试次数已达上限的事务
     */
    List<TransactionLog> findNeedCompensate();

    /**
     * 执行补偿
     *
     * @param transactionId 事务ID
     */
    void executeCompensate(String transactionId);

    /**
     * 查询事务执行状态
     *
     * @param businessKey 业务键
     * @return 事务日志
     */
    TransactionLog queryByBusinessKey(String businessKey);
}
```

---

## 六、执行流程详解

### 6.1 正常流程（全部成功）

```
时间轴：T0 ────────────────────────────────────────────────► T5

T0: 用户发起请求
    │
    ├─► AOP 拦截：创建 Saga 上下文 (txId=tx_001)
    │
T1: 执行业务方法
    │
    ├─► 本地插入数据 (sceneId=123)
    ├─► 注册远程调用1: thingui
    ├─► 注册远程调用2: topo
    │
T2: 本地事务提交前 (beforeCommit)
    │
    ├─► 插入补偿任务主表 (status=PENDING)
    ├─► 插入步骤明细表
    │    ├─ step1: thingui (PENDING)
    │    └─ step2: topo (PENDING)
    │
T3: 本地事务提交 ✅
    │
    ├─► 返回成功给用户 (用户体验：立即响应)
    │
T4: 异步执行远程调用 (afterCommit, 新线程)
    │
    ├─► 线程1: 调用森大屏 ✅
    │    ├─ 返回 thinguiId=456
    │    └─ 更新 step1.status=SUCCESS, result={"thinguiId":"456"}
    │
    ├─► 线程2: 调用 Topo ✅
    │    ├─ 返回 topoId=789
    │    └─ 更新 step2.status=SUCCESS, result={"topoId":"789"}
    │
T5: 所有步骤成功
    │
    └─► 条件更新事务为 SUCCESS，保留终态记录供审计、回查和归档

结果：数据一致，无需补偿
```

### 6.2 异常流程（部分失败 + 自动补偿）

```
时间轴：T0 ───────────────────────────────────────────────────────► T30min

T0: 用户发起请求
    │
    ├─► AOP 拦截：创建 Saga 上下文 (txId=tx_002)
    │
T1: 执行业务方法
    │
    ├─► 本地插入数据 (sceneId=456)
    ├─► 注册远程调用1: thingui
    ├─► 注册远程调用2: topo
    │
T2: 本地事务提交前
    │
    ├─► 插入补偿任务主表 (status=PENDING)
    ├─► 插入步骤明细表
    │
T3: 本地事务提交 ✅
    │
    ├─► 返回成功给用户
    │
T4: 异步执行远程调用
    │
    ├─► 线程1: 调用森大屏 ✅
    │    └─ 更新 step1.status=SUCCESS, result={"thinguiId":"789"}
    │
    ├─► 线程2: 调用 Topo ❌ (网络超时)
    │    └─ 更新 step2.status=FAILED, error="SocketTimeoutException"
    │
T5: 部分失败，等待重试
    │
    └─► 更新事务状态 = PARTIAL_SUCCESS

─────────────── 定时任务（每30秒） ───────────────

T5+30s: 第1次重试
    │
    ├─► 查询 step2 (status=FAILED, retry_count=0)
    ├─► 重新调用 Topo ❌ (仍然超时)
    └─► retry_count=1

T5+60s: 第2次重试
    ├─► retry_count=2 ❌

T5+90s: 第3次重试
    ├─► retry_count=3 ❌

T5+120s: 第4次重试
    ├─► retry_count=4 ❌

T5+150s: 第5次重试（最后一次）
    ├─► retry_count=5 ❌
    └─► 达到最大重试次数，触发补偿

T5+180s: 执行补偿（反向回滚）
    │
    ├─► 补偿步骤1: 删除森大屏
    │    ├─ 调用 thinguiClient.deleteScene("789") ✅
    │    └─ 更新 step1.status=COMPENSATED
    │
    ├─► 补偿步骤2: 删除本地数据
    │    ├─ 调用 sceneDao.deleteById("456") ✅
    │    └─ 记录本地删除日志
    │
    └─► 更新事务状态 = COMPENSATED

结果：所有数据已回滚，保持一致性
```

### 6.3 状态机转换

```
┌──────────┐
│ PENDING  │  初始状态（本地事务提交后）
└────┬─────┘
     │
     ▼
┌──────────┐
│EXECUTING │  远程调用执行中
└─┬──────┬─┘
  │      │
  │      ▼
  │  ┌────────────────┐
  │  │PARTIAL_SUCCESS │  部分成功，等待重试
  │  └───────┬────────┘
  │          │
  │          ▼
  │      ┌─────────────┐
  │      │COMPENSATING │  补偿中
  │      └──────┬──────┘
  │             │
  │             ▼
  │      ┌────────────┐
  │      │COMPENSATED │  已补偿（终态）
  │      └────────────┘
  │
  ▼
┌──────────┐
│ SUCCESS  │  全部成功（终态）
└──────────┘
```

---

## 七、使用指南

### 7.1 Maven 依赖

```xml
<!-- 分布式事务补偿插件 -->
<dependency>
    <groupId>com.github.lvdaxianer</groupId>
    <artifactId>saga-compensation-spring-boot-starter</artifactId>
    <version>0.1.0-SNAPSHOT</version>
</dependency>
```

### 7.2 配置文件

```yaml
# application.yml

# MySQL 模式（默认）：与发起方业务库同一写数据源、同一 Spring 本地事务
saga:
  transaction:
    enabled: true
    store-type: mysql
    max-retry: 5
    lease-duration: 5m
    scheduler-enabled: true
    scheduler-delay: 30s
    scheduler-initial-delay: 30s
    schema-initialize: false
```

```yaml
# Elasticsearch 7.10.2 模式：业务方负责 RestHighLevelClient 和索引权限
saga:
  transaction:
    enabled: true
    store-type: elasticsearch
    max-retry: 5
    lease-duration: 5m
    scheduler-enabled: true
    scheduler-delay: 30s
    scheduler-initial-delay: 30s
    elasticsearch:
      transaction-index: saga-transaction-log-v1
      step-index: saga-transaction-step-v1
      schema-initialize: false
```

两个存储模式只能二选一。MySQL 模式的业务写入和 Saga 记录同提交或同回滚；ES 模式不允许把
`SagaWorkflowLauncher` 放进活动 Spring 本地事务，业务必须自行设计中间状态、补偿和回查。

### 7.3 云瞰用户创建：ES 最终一致性示例

云瞰向森大屏创建用户时，Saga 框架不理解“用户”业务，只提供事务、步骤、租约、重试、终态通知
和恢复能力；云瞰后端负责把终态映射为用户生命周期。

```text
1. 云瞰生成用户 ID、稳定业务键和 sagaTransactionId
2. 云瞰 ES 写入用户：syncStatus=PENDING_SYNC，前端可见但不可操作
3. Starter 在 saga-transaction-* 索引创建/复用 USER_CREATE:THINGUI 工作流
4. 正向步骤调用森大屏创建用户，使用 transactionId:stepId 幂等键
5a. 森大屏成功 -> Saga SUCCESS -> 条件更新云瞰用户为 ACTIVE，清空 sagaTransactionId
5b. 可重试失败 -> Saga PENDING，保留 PENDING_SYNC，等待恢复调度
5c. 重试耗尽 -> Saga FAILED -> 条件删除仍关联该事务的 PENDING_SYNC 用户
6. 终态通知丢失 -> 集群 leader 或单实例批量回查 ES 终态，重放 5a/5c 的幂等条件写入
```

中间状态对前端可见，列表显示“同步中”，但不允许登录、编辑、禁用、删除、解锁或查看验证码。
`ACTIVE` 与删除均以 `用户ID + sagaTransactionId + PENDING_SYNC` 为条件更新，故重复终态事件、
多节点并发和回查重放不会改变已完成的最终结果。

### 7.4 代码示例

#### 示例1：创建订单（扣库存 + 创建支付单）

```java
@Service
public class OrderService {

    @Autowired
    private OrderDao orderDao;

    @Autowired
    private InventoryClient inventoryClient;

    @Autowired
    private PaymentClient paymentClient;

    /**
     * 创建订单
     *
     * @param dto 订单DTO
     * @return 订单实体
     * @author lvdaxianer@yeah.net
     * @date 2026-09-04
     */
    @SagaTransaction(
        name = "createOrder",
        businessKey = "#result.id",
        asyncRemote = true,
        maxRetry = 5
    )
    @Transactional(rollbackFor = Exception.class)
    public OrderEntity createOrder(OrderDTO dto) {

        // 1. 本地插入订单（在 Spring 事务内）
        OrderEntity order = new OrderEntity();
        order.setProductId(dto.getProductId());
        order.setQuantity(dto.getQuantity());
        order.setUserId(dto.getUserId());
        orderDao.insert(order);

        // 2. 获取 Saga 上下文
        SagaContext sagaContext = SagaContextHolder.get();

        // 3. 注册远程调用1：扣减库存
        sagaContext.registerRemote("inventory",
            () -> {
                // 执行：扣减库存
                InventoryResponse response = inventoryClient.deduct(
                    dto.getProductId(),
                    dto.getQuantity()
                );
                log.info("库存扣减成功: orderId={}, productId={}, quantity={}",
                    order.getId(), dto.getProductId(), dto.getQuantity());
                return response.getLockId();
            },
            lockId -> {
                // 补偿：释放库存
                inventoryClient.release(lockId);
                log.info("库存释放成功: lockId={}", lockId);
            }
        );

        // 4. 注册远程调用2：创建支付单
        sagaContext.registerRemote("payment",
            () -> {
                // 执行：创建支付单
                PaymentResponse response = paymentClient.create(
                    order.getId(),
                    order.getTotalAmount()
                );
                log.info("支付单创建成功: orderId={}, paymentId={}",
                    order.getId(), response.getPaymentId());
                return response.getPaymentId();
            },
            paymentId -> {
                // 补偿：取消支付单
                paymentClient.cancel(paymentId);
                log.info("支付单取消成功: paymentId={}", paymentId);
            }
        );

        // 5. 返回订单实体
        return order;
    }
}
```

#### 示例2：用户注册（发邮件 + 初始化积分）

```java
@Service
public class UserService {

    @Autowired
    private UserDao userDao;

    @Autowired
    private EmailClient emailClient;

    @Autowired
    private PointsClient pointsClient;

    /**
     * 用户注册
     *
     * @param dto 注册DTO
     * @return 用户实体
     * @author lvdaxianer@yeah.net
     * @date 2026-09-04
     */
    @SagaTransaction(
        name = "userRegister",
        businessKey = "#result.id",
        asyncRemote = true,
        maxRetry = 3
    )
    @Transactional(rollbackFor = Exception.class)
    public UserEntity register(UserRegisterDTO dto) {

        // 1. 本地插入用户
        UserEntity user = new UserEntity();
        user.setUsername(dto.getUsername());
        user.setEmail(dto.getEmail());
        user.setPassword(passwordEncoder.encode(dto.getPassword()));
        userDao.insert(user);

        SagaContext sagaContext = SagaContextHolder.get();

        // 2. 注册远程调用1：发送欢迎邮件
        sagaContext.registerRemote("email",
            () -> {
                emailClient.sendWelcome(user.getEmail(), user.getUsername());
                log.info("欢迎邮件发送成功: userId={}, email={}",
                    user.getId(), user.getEmail());
                return user.getEmail();
            },
            email -> {
                // 邮件无法撤回，记录日志即可
                log.warn("用户注册失败，已发送的欢迎邮件无法撤回: email={}", email);
            }
        );

        // 3. 注册远程调用2：初始化积分账户
        sagaContext.registerRemote("points",
            () -> {
                PointsResponse response = pointsClient.initialize(
                    user.getId(),
                    100  // 新用户赠送100积分
                );
                log.info("积分账户初始化成功: userId={}, pointsId={}",
                    user.getId(), response.getPointsId());
                return response.getPointsId();
            },
            pointsId -> {
                // 补偿：删除积分账户
                pointsClient.delete(pointsId);
                log.info("积分账户删除成功: pointsId={}", pointsId);
            }
        );

        return user;
    }
}
```

#### 示例3：删除资源（先删除远程，再删除本地）

```java
@Service
public class ResourceService {

    @Autowired
    private ResourceDao resourceDao;

    @Autowired
    private StorageClient storageClient;

    @Autowired
    private CdnClient cdnClient;

    /**
     * 删除资源
     *
     * @param resourceId 资源ID
     * @author lvdaxianer@yeah.net
     * @date 2026-09-04
     */
    @SagaTransaction(
        name = "deleteResource",
        businessKey = "#resourceId",
        asyncRemote = false,  // 删除操作建议同步执行
        maxRetry = 3
    )
    @Transactional(rollbackFor = Exception.class)
    public void deleteResource(String resourceId) {

        // 1. 查询资源信息
        ResourceEntity resource = resourceDao.selectById(resourceId);
        if (resource == null) {
            throw new ServiceException("资源不存在");
        }

        SagaContext sagaContext = SagaContextHolder.get();

        // 2. 注册远程调用1：删除对象存储文件
        if (StrUtil.isNotBlank(resource.getStorageKey())) {
            sagaContext.registerRemote("storage",
                () -> {
                    storageClient.delete(resource.getStorageKey());
                    return resource.getStorageKey();
                },
                storageKey -> {
                    // 补偿：记录日志（文件已删除，无法恢复）
                    log.error("文件删除后补偿失败，需人工介入: storageKey={}", storageKey);
                }
            );
        }

        // 3. 注册远程调用2：刷新 CDN 缓存
        if (StrUtil.isNotBlank(resource.getCdnUrl())) {
            sagaContext.registerRemote("cdn",
                () -> {
                    cdnClient.purge(resource.getCdnUrl());
                    return resource.getCdnUrl();
                },
                cdnUrl -> {
                    // CDN 缓存刷新失败，记录日志
                    log.warn("CDN缓存刷新失败: cdnUrl={}", cdnUrl);
                }
            );
        }

        // 4. 删除本地数据
        resourceDao.deleteById(resourceId);

        log.info("资源删除成功: resourceId={}", resourceId);
    }
}
```

#### 示例4：查询事务执行状态（通用）

```java
@RestController
@RequestMapping("/api/transaction")
public class TransactionController {

    @Autowired
    private CompensationService compensationService;

    /**
     * 查询事务状态
     *
     * @param businessKey 业务键（如 orderId、userId、resourceId）
     * @return 事务执行结果
     */
    @GetMapping("/status")
    public ApiResult<TransactionStatusVO> queryStatus(
            @RequestParam String businessKey) {

        TransactionLog log = compensationService.queryByBusinessKey(businessKey);

        if (log == null) {
            return ApiResult.success(TransactionStatusVO.builder()
                .status("NOT_FOUND")
                .message("未找到事务记录")
                .build());
        }

        return ApiResult.success(TransactionStatusVO.builder()
            .transactionId(log.getId())
            .businessKey(log.getBusinessKey())
            .status(log.getStatus())
            .retryCount(log.getRetryCount())
            .errorMessage(log.getErrorMessage())
            .createdAt(log.getCreatedAt())
            .build());
    }
}
```

---

## 八、部署方案

### 8.1 单机部署

```yaml
# 适用场景：开发环境、测试环境

配置：
- 本地事务：Spring Transaction
- 补偿调度：Spring @Scheduled
- 无需分布式锁

优点：
- 部署简单
- 无外部依赖

缺点：
- 单点故障
- 不支持水平扩展
```

### 8.2 集群部署（推荐）

```yaml
# 适用场景：生产环境

架构：
┌──────────┐     ┌──────────┐     ┌──────────┐
│  Node 1  │     │  Node 2  │     │  Node 3  │
│  (Leader)│     │(Follower)│     │(Follower)│
└────┬─────┘     └────┬─────┘     └────┬─────┘
     │                │                │
     └────────────────┴────────────────┘
                      │
                      ▼
              ┌──────────────┐
              │  Redis       │  (分布式锁)
              └──────────────┘
                      │
                      ▼
              ┌────────────────────────────────────┐
              │ MySQL 业务库 或 Elasticsearch 7.10.2 │
              │ （按 store-type 选择唯一状态源）     │
              └────────────────────────────────────┘

配置：
saga:
  transaction:
    scheduler-enabled: true
    scheduler-delay: 30s
    scheduler-initial-delay: 30s

状态源部署：
1. MySQL：`saga_transaction_log`、`saga_transaction_step` 与发起方业务表同逻辑库、同写数据源；不得为每个节点建表，也不得跨服务共用中心表。
2. Elasticsearch：业务索引与 `saga-transaction-log-v1`、`saga-transaction-step-v1` 位于同一 ES 7.10.2 集群；索引由部署迁移预创建，所有节点访问同一集群。
3. 不允许同一个 Saga 同时写 MySQL 和 ES；`store-type` 是状态源选择，不是多活副本策略。
4. Redis 仅可协调扫描锁；MySQL 表或 ES 索引才是可恢复的事务状态源。

实现原理：
1. 集群可使用调度锁或 leader 门禁缩小重复扫描；未获得门禁的节点跳过本轮。
2. 获得门禁的节点从选定状态源批量读取待处理事务，并以租约和状态条件更新（CAS）抢占具体任务；锁异常释放、人工重试或后续改为分片调度时也不会由多个节点同时推进同一状态。
3. MySQL 从主库读取；ES 使用索引查询和批量 `mget`，避免逐条读取造成 N+1 请求。
4. 节点故障后，下一轮根据租约超时接管；远端结果不确定的步骤先对账或转人工处理。
5. 对 ES 业务中间态，额外运行终态回查任务；它只重放持久化终态，不能用内存事件作为唯一事实来源。
```

### 8.3 监控和告警

#### 监控指标

```java
/**
 * 分布式事务监控指标
 */
public interface TransactionMetrics {

    // 事务总数
    Counter transactionTotal();

    // 成功数
    Counter transactionSuccess();

    // 失败数
    Counter transactionFailed();

    // 补偿数
    Counter compensationTotal();

    // 平均执行时间
    Timer executionTime();

    // 重试次数分布
    Histogram retryDistribution();
}
```

#### Prometheus 暴露

```yaml
# actuator 配置
management:
  endpoints:
    web:
      exposure:
        include: prometheus,health,info
  metrics:
    tags:
      application: ${spring.application.name}
```

#### Grafana 监控面板

```
面板1: 事务执行概览
- 总事务数
- 成功率
- 失败率
- 补偿率

面板2: 执行时间趋势
- 平均执行时间
- P95 执行时间
- P99 执行时间

面板3: 补偿任务状态
- 待补偿任务数
- 补偿中任务数
- 补偿失败任务数

面板4: 告警列表
- 补偿失败告警
- 重试次数超限告警
- 执行时间超时告警
```

#### 钉钉告警

```java
/**
 * 告警内容示例
 */
{
  "msgtype": "markdown",
  "markdown": {
    "title": "分布式事务补偿失败",
    "text": "## 分布式事务补偿失败\n\n" +
            "- 事务ID: tx_20260904100001\n" +
            "- 业务键: scene_123\n" +
            "- 事务名称: createScene\n" +
            "- 失败步骤: create_topo\n" +
            "- 重试次数: 5/5\n" +
            "- 错误信息: SocketTimeoutException: Read timed out\n" +
            "- 时间: 2026-09-04 10:05:30\n\n" +
            "> 需要人工介入处理"
  }
}
```

---

## 九、性能优化

### 9.1 批量补偿

当补偿任务数量较多时，采用批量处理：

```java
/**
 * 批量补偿优化
 */
@Scheduled(fixedDelay = 30000)
public void batchCompensate() {
    // 1. 批量查询（限制 100 条）
    List<TransactionLog> needCompensate = txDao.findNeedCompensate(100);

    // 2. 分批处理（每批 10 个）
    Lists.partition(needCompensate, 10).forEach(batch -> {
        // 并行执行补偿
        batch.parallelStream().forEach(tx -> {
            try {
                compensate(tx);
            } catch (Exception e) {
                log.error("批量补偿失败", e);
            }
        });
    });
}
```

### 9.2 异步日志

补偿任务日志采用异步写入，避免阻塞主流程：

```xml
<!-- logback-spring.xml -->
<appender name="ASYNC_COMPENSATION" class="ch.qos.logback.classic.AsyncAppender">
    <queueSize>1000</queueSize>
    <discardingThreshold>0</discardingThreshold>
    <appender-ref ref="COMPENSATION_FILE"/>
</appender>
```

### 9.3 数据库索引优化

```sql
-- 补偿任务查询优化
CREATE INDEX idx_status_retry ON saga_transaction_log(status, retry_count);
CREATE INDEX idx_status_created ON saga_transaction_step(status, created_at);

-- 分区表（按月分区）
ALTER TABLE saga_transaction_log PARTITION BY RANGE (TO_DAYS(created_at)) (
    PARTITION p202601 VALUES LESS THAN (TO_DAYS('2026-02-01')),
    PARTITION p202602 VALUES LESS THAN (TO_DAYS('2026-03-01')),
    ...
);
```

### 9.4 历史数据归档

```java
/**
 * 历史数据归档（定期执行）
 */
@Scheduled(cron = "0 0 2 * * ?") // 每天凌晨2点
public void archiveHistory() {
    // 归档 30 天前的已完成/已补偿事务
    LocalDateTime archiveTime = LocalDateTime.now().minusDays(30);

    // 1. 导出到归档表
    txDao.archiveCompleted(archiveTime);

    // 2. 删除主表数据
    txDao.deleteCompleted(archiveTime);

    log.info("历史数据归档完成: archiveTime={}", archiveTime);
}
```

---

## 十、FAQ 常见问题

### Q1: 为什么不使用 Seata？

**A**: Seata 是优秀的分布式事务框架，但对于我们的场景：
- **过重**：需要独立部署 TC（Transaction Coordinator）服务器
- **侵入性大**：需要修改数据源配置，引入 undo_log 表
- **学习成本高**：团队需要学习 AT、TCC、Saga 三种模式
- **不支持 HTTP**：Seata 主要面向 RPC（Dubbo、gRPC），对 HTTP 支持不友好

### Q2: 如果补偿也失败了怎么办？

**A**: 补偿失败属于极端情况，处理策略：
1. **记录详细日志**：包括失败原因、上下文信息
2. **发送告警**：钉钉/邮件通知运维人员
3. **人工介入**：通过管理后台查看失败详情，手动处理
4. **补偿重试**：对于网络抖动导致的失败，可以手动触发重试

### Q3: 如何保证幂等性？

**A**: 虽然远程接口不支持幂等，但框架层通过以下机制保证：
1. **状态机控制**：每个步骤只能从 PENDING → EXECUTING → SUCCESS/FAILED
2. **数据库唯一约束**：`(transaction_id, step_order)` 唯一索引
3. **分布式锁**：集群环境下，执行前先获取锁
4. **CAS 更新**：更新步骤状态时，使用 `WHERE status = 'PENDING'` 条件

### Q4: 用户如何知道远程调用的结果？

**A**: 有两种方式：
1. **轮询接口**：前端定时调用 `/api/transaction/status` 查询状态
2. **WebSocket 推送**：后端在远程调用完成后，主动推送通知给前端
3. **异步回调**：提供回调接口，远程调用完成后调用业务方的 callback URL

### Q5: 支持嵌套事务吗？

**A**: **不支持**。原因：
- 嵌套事务会导致补偿逻辑复杂化
- 容易产生死锁和状态混乱
- 建议拆分为多个独立的事务

### Q6: 如何测试补偿逻辑？

**A**: 提供测试工具：

```java
@SpringBootTest
public class CompensationTest {

    @Autowired
    private CompensationService compensationService;

    @Test
    public void testCompensate() {
        // 1. 模拟远程调用失败
        String transactionId = "test_tx_001";

        // 2. 手动触发补偿
        compensationService.executeCompensate(transactionId);

        // 3. 验证补偿结果
        TransactionLog log = compensationService.queryById(transactionId);
        assertEquals("COMPENSATED", log.getStatus());
    }
}
```

### Q7: 如何处理并发冲突？

**A**: 场景：用户在远程调用还在执行时，又发起了修改或删除操作。

解决方案：
1. **前端锁定**：创建场景后，按钮置灰，显示"创建中..."
2. **后端检查**：修改/删除前，先查询事务状态，如果是 EXECUTING 则拒绝操作
3. **乐观锁**：场景表增加 `version` 字段，更新时检查版本号

---

## 十一、总结

### 11.1 核心优势

| 特性 | 说明 |
|------|------|
| **低侵入性** | 只需加注解，改动 < 10 行代码 |
| **最终一致性** | 异步执行，用户体验好 |
| **自动补偿** | 失败自动重试 + 回滚，无需人工介入 |
| **可观测** | 完整的日志、监控、告警 |
| **高性能** | 并行执行远程调用，支持批量补偿 |
| **易扩展** | 插件化设计，支持自定义补偿逻辑 |

### 11.2 适用场景

✅ **适合**：
- 本地数据库 + HTTP 远程调用
- 允许最终一致性（短暂不一致）
- 远程服务提供补偿接口（如删除、取消）
- Spring Boot 技术栈

❌ **不适合**：
- 强一致性要求（如支付核心流程）
- 远程服务不支持补偿操作
- 纯微服务架构（建议使用 Seata）
- 对性能要求极高的场景（RT < 10ms）

### 11.3 实施计划

**第一阶段（1周）**：
- 搭建框架基础结构
- 实现 AOP 拦截和 Saga 上下文
- 实现数据库表和 DAO 层

**第二阶段（1周）**：
- 实现异步执行和补偿调度
- 集成分布式锁（可选）
- 编写单元测试

**第三阶段（1周）**：
- 编写示例代码和文档
- 集成监控和告警
- 部署到测试环境

**第四阶段（1周）**：
- 压力测试和性能优化
- 发布 Maven 包
- 编写完整使用文档

---

## 十二、与标准 Saga 模式的对比

### 12.1 标准 Saga 模式

Saga 模式是由 Hector Garcia-Molina 和 Kenneth Salem 在 1987 年提出的分布式事务解决方案。

**核心思想**：
```
长事务 = 一系列本地事务 T1, T2, ..., Tn
每个本地事务 Ti 都有对应的补偿事务 Ci
如果 Ti 失败，则执行 Cn-1, Cn-2, ..., C1（反向补偿）
```

**两种实现方式**：

#### 方式1：Choreography（事件编排）
```
服务A → 完成 → 发布事件 → 服务B监听 → 完成 → 发布事件 → 服务C监听
       ↓                        ↓                        ↓
     补偿C ← 失败事件 ← 补偿B ← 失败事件 ← 补偿A
```

特点：
- ✅ 去中心化，无协调者
- ✅ 松耦合
- ❌ 难以追踪流程
- ❌ 循环依赖风险

#### 方式2：Orchestration（中心编排）
```
      ┌─────────────────┐
      │  Orchestrator   │  (协调器)
      │  (协调所有服务)  │
      └────┬───┬───┬─────┘
           │   │   │
       ┌───┴┐ ┌┴───┐ ┌┴───┐
       │服务A│ │服务B│ │服务C│
       └────┘ └────┘ └────┘
```

特点：
- ✅ 流程清晰，易于追踪
- ✅ 集中控制补偿逻辑
- ❌ 协调器是单点
- ❌ 协调器需要持久化状态

### 12.2 本框架 vs 标准 Saga

| 对比维度 | 标准 Saga（Orchestration） | 本框架 |
|---------|---------------------------|--------|
| **架构模式** | 独立的协调器服务 | 嵌入式框架（Spring AOP） |
| **部署方式** | 需要独立部署 TC 服务器 | 无需额外部署，随应用启动 |
| **事务范围** | 跨多个微服务 | 单体应用内（本地事务 + 远程调用） |
| **协调方式** | 协调器调用各个服务 | AOP 拦截 + 数据库记录状态 |
| **状态持久化** | 协调器的内存/数据库 | MySQL 事务表或 ES 7.10.2 双索引（由 `store-type` 选择） |
| **通信协议** | RPC（Dubbo/gRPC）或消息队列 | HTTP/REST（直接调用） |
| **学习成本** | 高（需要理解 Saga、协调器、事件） | 低（只需加注解） |
| **侵入性** | 中等（需要实现接口） | 低（只需加注解） |
| **适用场景** | 微服务架构 | 单体或部分微服务化 |
| **优势** | 适合复杂分布式场景 | 轻量、易用、快速集成 |
| **劣势** | 重量级，需要运维协调器 | 不适合纯微服务架构 |

### 12.3 核心差异

#### 差异1：无独立协调器

**标准 Saga**：
```
┌─────────────┐
│ Coordinator │ (独立部署)
└──────┬──────┘
       │
   调用各服务
       │
┌──────┴──────┐
│   服务集群   │
└─────────────┘
```

**本框架**：
```
┌─────────────────────┐
│   应用内 AOP 拦截    │ (嵌入式)
│  + 补偿任务定时器    │
└─────────────────────┘
           │
     直接 HTTP 调用
           │
   ┌───────┴────────┐
   │   远程服务      │
   └────────────────┘
```

#### 差异2：状态管理

**标准 Saga**：
- 协调器内存 + 持久化存储（如 Seata 的 global_table）
- 需要处理协调器宕机恢复

**本框架**：
- MySQL 模式：本地业务库中的 `saga_transaction_log` 与 `saga_transaction_step`，本地事务保证原子提交
- ES 模式：ES 7.10.2 的事务头与步骤双索引，中间状态、下游幂等、CAS、重试和回查共同保证最终一致性
- 应用重启或终态事件丢失后，通过恢复调度与终态回查从持久化状态恢复

#### 差异3：编程模型

**标准 Saga（以 Seata 为例）**：
```java
@GlobalTransactional
public void createOrder() {
    // 需要配置 undo_log 表
    // 需要修改数据源配置
    // 需要运行 Seata Server
    orderService.create();
    inventoryService.deduct();
    paymentService.create();
}
```

**本框架**：
```java
@SagaTransaction
public void createOrder() {
    // 无需修改数据源
    // 无需独立服务器
    // 直接注册补偿逻辑
    orderDao.insert(order);

    sagaContext.registerRemote("inventory",
        () -> inventoryClient.deduct(),
        lockId -> inventoryClient.release(lockId)
    );
}
```

### 12.4 何时选择本框架，何时选择标准 Saga？

#### 选择本框架：

✅ 单体应用或单体转微服务过渡期
✅ 远程调用数量少（< 5个服务）
✅ 团队对分布式事务经验不足
✅ 需要快速上线，不想维护额外组件
✅ 主要是 HTTP/REST 调用

#### 选择标准 Saga（Seata）：

✅ 纯微服务架构（10+ 服务）
✅ 使用 RPC 框架（Dubbo、gRPC）
✅ 需要跨多个数据库的分布式事务
✅ 团队有运维能力（可以运维 Seata Server）
✅ 需要 AT 模式（自动回滚）

### 12.5 渐进式演进路径

```
阶段1: 单体应用
  └─ 使用本框架（嵌入式）

阶段2: 部分微服务化
  └─ 继续使用本框架（单体内） + 少量 RPC 调用

阶段3: 完全微服务化
  └─ 迁移到 Seata（独立协调器）
```

---

## 十三、框架包结构设计

为了便于开发和使用，建议采用以下包结构：

```
distributed-transaction-framework/
├── pom.xml
├── README.md
└── src/main/java/com/github/lvdaxianer/dtx/
    ├── annotation/
    │   └── SagaTransaction.java       (注解定义)
    │
    ├── core/
    │   ├── SagaContext.java                  (Saga 上下文接口)
    │   ├── SagaContextImpl.java              (Saga 上下文实现)
    │   ├── SagaContextHolder.java            (ThreadLocal 持有者)
    │   ├── RemoteAction.java                 (远程调用接口)
    │   └── CompensateAction.java             (补偿接口)
    │
    ├── aop/
    │   └── SagaTransactionAspect.java (AOP 拦截器)
    │
    ├── entity/
    │   ├── TransactionLog.java               (事务日志实体)
    │   └── TransactionStep.java              (事务步骤实体)
    │
    ├── dao/
    │   ├── TransactionLogDao.java            (事务日志DAO)
    │   └── TransactionStepDao.java           (事务步骤DAO)
    │
    ├── service/
    │   ├── CompensationService.java          (补偿服务接口)
    │   └── CompensationServiceImpl.java      (补偿服务实现)
    │
    ├── scheduler/
    │   └── CompensationScheduler.java        (补偿定时任务)
    │
    ├── config/
    │   ├── SagaTransactionProperties.java  (配置属性)
    │   └── SagaTransactionAutoConfig.java  (自动配置)
    │
    ├── exception/
    │   ├── TransactionException.java         (事务异常)
    │   └── CompensationException.java        (补偿异常)
    │
    ├── util/
    │   ├── JsonUtil.java                     (JSON 工具)
    │   └── ReflectionUtil.java               (反射工具)
    │
    └── monitor/
        ├── TransactionMetrics.java           (监控指标)
        └── AlertService.java                 (告警服务)
```

---

**文档版本历史**：
- v1.0 (2026-09-04): 初始版本，通用框架设计
- v1.1 (2026-09-06): 补充 MySQL/Elasticsearch 7.10.2 双存储、集群状态源与云瞰用户最终一致性方案

**联系方式**：
- 作者邮箱: lvdaxianer@yeah.net
- GitHub: (待发布)
- 文档路径: /Users/lvdaxianer/workspace/my/project/Axiom
