## 1. Production architecture document

- [x] 1.1 Create `docs/superpowers/specs/2026-08-26-doris-time-series-architecture-design.md` with production topology, query-oriented Doris DDL, data lifecycle and migration workflow, Tarsier query routes, cache consistency rules, capacity formula, implementation prerequisites, and exclusions. Task boundary: architecture documentation only; allowed files are this document and this OpenSpec change; out of scope are application code, cluster configuration, benchmark scripts, and existing OpenSpec changes. Agent dispatch: direct main-agent execution because the task is one cohesive document with no safely separable write scope. TDD is not applicable because no production behavior is implemented. Focused verification: `openspec validate production-time-series-doris-architecture --strict`, placeholder scan over the new change and document, and `git diff --check`. Broader verification: review all PDF-derived endpoint categories against the document and inspect the full diff. Handoff evidence: validated OpenSpec output, placeholder scan, whitespace check, and review record.
