# 方案库规范

## 1. 目录原则

Axiom 按“一个目录 = 一个解决方案”组织内容。每个解决方案目录应能独立表达背景、目标、落地步骤、验证方式和参考资料。

推荐结构：

```text
Axiom/
  README.md
  方案库规范.md

  解决方案名称/
    README.md
    references/
      配置示例.ext
      操作步骤.md
      排障记录.md
      官方资料.md
```

## 2. 命名规则

1. 方案目录使用中文名称，直接表达业务含义。
2. 每个方案目录必须有 `README.md`，作为该方案的主入口。
3. 每个方案目录可以有 `references/`，用于放配置、日志样例、操作步骤、排障记录、外部资料摘要等辅助材料。
4. 配置文件保留真实后缀，例如 `.toml`、`.conf`、`.json`、`.yaml`。
5. 根目录 `README.md` 只做方案索引和简短说明，不承载具体方案细节。

## 3. README 推荐章节

方案主文档建议包含：

1. 背景
2. 目标
3. 总体链路或架构
4. 核心配置或实现
5. 部署步骤
6. 验证方式
7. 风险与边界
8. 排障方式
9. 后续演进
10. 参考资料

章节可按方案复杂度增减，但需要保证读者只看 `README.md` 就能理解方案怎么落地。

## 4. references 使用规则

`references/` 用于存放主文档不适合完整展开的资料：

1. 最终可复制配置
2. Nginx、Vector、Docker、systemd 等配置片段
3. 示例日志和请求响应
4. 排障记录
5. 操作步骤
6. 官方文档链接整理
7. 早期调研材料

如果某份资料是最终落地必需的，应在方案 `README.md` 中明确链接到它。

## 5. 更新根目录索引

新增或调整方案时，需要同步更新根目录 `README.md` 的方案表格：

```md
| [方案名称](./方案名称/README.md) | 一句话说明方案解决什么问题。 | 状态 |
```

状态建议使用：

1. `调研中`
2. `方案设计`
3. `验证中`
4. `已验证`
5. `已废弃`

## 6. 忽略规则

仓库只保存可复用方案、配置示例和必要参考资料，不保存本地运行产物、日志、缓存、密钥和临时文件。

敏感信息不要写入方案文档或 references。如果必须展示请求头、token、cookie 等字段，只保留字段名，并使用占位符。
